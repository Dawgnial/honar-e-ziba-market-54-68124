# راهنمای جستجوی پنل مدیریت

این سند نحوه استفاده از قابلیت جستجو در پنل مدیریت را توضیح می‌دهد.

## ویژگی‌های جستجو

### 1. جستجوی محصولات (Admin Products)

قابلیت جستجو در صفحه محصولات امکانات زیر را دارد:

**جستجو در:**
- ✅ نام محصول (title)
- ✅ دسته‌بندی محصول (category)
- ✅ توضیحات محصول (description)

**ویژگی‌های خاص:**
- 🔤 پشتیبانی از متن فارسی با نرمال‌سازی
- ⚡ Debouncing برای جلوگیری از جستجوهای زیاد (300ms)
- 📊 نمایش تعداد نتایج زنده
- 🔄 بازگشت خودکار به صفحه اول هنگام جستجو
- 💾 استفاده از useMemo برای بهینه‌سازی

### 2. جستجوی دسته‌بندی‌ها (Admin Categories)

قابلیت جستجو در صفحه دسته‌بندی‌ها:

**جستجو در:**
- ✅ نام دسته‌بندی (title)

**ویژگی‌های خاص:**
- 🔤 پشتیبانی از متن فارسی با نرمال‌سازی
- ⚡ Debouncing برای جلوگیری از جستجوهای زیاد (300ms)
- 📊 نمایش تعداد نتایج زنده
- 🔄 بازگشت خودکار به صفحه اول هنگام جستجو
- 💾 استفاده از useMemo برای بهینه‌سازی

## نرمال‌سازی متن فارسی

برای جستجوی بهتر، سیستم متن‌های فارسی را نرمال‌سازی می‌کند:

```typescript
const normalizeText = (text: string): string => {
  return text
    .toLowerCase()              // تبدیل به حروف کوچک
    .replace(/ی/g, 'ي')        // یای فارسی به عربی
    .replace(/ک/g, 'ك')        // کاف فارسی به عربی
    .replace(/ة/g, 'ه')        // تاء مربوطه به هاء
    .replace(/آ/g, 'ا')        // الف با کلاه به الف ساده
    .trim();
};
```

**مثال:**
- جستجوی "قوري" و "قوری" هر دو کار می‌کنند
- جستجوی "كتاب" و "کتاب" هر دو کار می‌کنند

## نحوه استفاده

### مثال 1: جستجوی محصول با نام

```
ورودی: "قوری"
نتیجه: تمام محصولاتی که کلمه "قوری" در نام، دسته‌بندی یا توضیحات آن‌ها هست
```

### مثال 2: جستجوی با دسته‌بندی

```
ورودی: "ظروف"
نتیجه: تمام محصولاتی که در دسته‌بندی "ظروف" قرار دارند
```

### مثال 3: جستجوی با توضیحات

```
ورودی: "دست‌ساز"
نتیجه: تمام محصولاتی که کلمه "دست‌ساز" در توضیحات آن‌ها وجود دارد
```

## عیب‌یابی

اگر جستجو کار نمی‌کند:

### 1. بررسی Console Logs

در Developer Tools (F12) > Console، باید لاگ‌های زیر را ببینید:

```
🔍 Admin Products Search: { totalProducts: 45, searchTerm: "قوری", hasSearch: true }
✓ Match found: قوری مسی
✓ Match found: قوری سنتی
📊 Filtered results: 2
```

### 2. بررسی داده‌ها

اگر `totalProducts: 0` است، یعنی محصولی از دیتابیس بارگذاری نشده:
- بررسی کنید که محصولی در دیتابیس وجود دارد
- بررسی کنید که RLS policies درست تنظیم شده‌اند
- Network tab را برای خطاهای API چک کنید

### 3. بررسی متن جستجو

اگر هیچ نتیجه‌ای پیدا نمی‌شود:
- مطمئن شوید که متن جستجو درست تایپ شده
- از حروف فارسی استاندارد استفاده کنید
- امتحان کنید بخشی از کلمه را جستجو کنید (مثل "قور" به جای "قوری")

### 4. پاک کردن Cache

گاهی cache مرورگر مشکل ایجاد می‌کند:
```
Ctrl+Shift+R (Windows/Linux)
Cmd+Shift+R (Mac)
```

## Performance

### Debouncing
جستجو با تاخیر 300ms انجام می‌شود تا از جستجوهای زیاد جلوگیری شود:
- کاربر تایپ می‌کند: "ق" → هیچ اتفاقی
- کاربر تایپ می‌کند: "قو" → هیچ اتفاقی
- کاربر تایپ می‌کند: "قوری" → 300ms بعد جستجو شروع می‌شود

### Memoization
نتایج فیلتر شده با `useMemo` cache می‌شوند تا فقط وقتی ضروری است دوباره محاسبه شوند.

## مثال‌های واقعی

### سناریو 1: جستجوی محصول خاص
```
1. به صفحه "مدیریت محصولات" بروید
2. در کادر جستجو "قوری مسی" تایپ کنید
3. 300ms بعد، نتایج فیلتر می‌شوند
4. محصولات مرتبط نمایش داده می‌شوند
```

### سناریو 2: جستجوی دسته‌بندی
```
1. به صفحه "مدیریت دسته‌بندی‌ها" بروید
2. در کادر جستجو "ظروف" تایپ کنید
3. فقط دسته‌بندی‌هایی که "ظروف" در نامشان هست نمایش داده می‌شوند
```

### سناریو 3: پاک کردن جستجو
```
1. متن را در کادر جستجو حذف کنید
2. تمام محصولات/دسته‌بندی‌ها دوباره نمایش داده می‌شوند
```

## API Reference

### normalizeText(text: string): string
نرمال‌سازی متن فارسی برای جستجوی بهتر

**پارامترها:**
- `text`: متن ورودی

**خروجی:**
- متن نرمال‌سازی شده

### useDebounce<T>(value: T, delay: number): T
Debounce کردن مقدار برای بهبود performance

**پارامترها:**
- `value`: مقدار ورودی
- `delay`: تاخیر به میلی‌ثانیه

**خروجی:**
- مقدار debounce شده
